<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">ATS</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <form class="navbar-form navbar-nav">
        <div class="form-group">
          <input type="text" class="form-control" placeholder="Search">
        </div>
        <button type="submit" class="btn btn-default">Submit</button>
    </form> -->

    {% if currentUser %}
      <ul class="nav navbar-nav navbar-right my-nav-bar-right ">
        {%if logged_in%}
          <li>
            <form class="my-nav-form-pad" action="/create/ticket/{{project.id}}" method="get">
              <button type="submit" class="btn btn-primary">New Ticket</button>
            </form>

          </li>
          {%endif%}
          {%if owner%}
            <li>
              <form class="my-nav-form-pad" action="/update/project/{{project.id}}" method="get">
                <button type="submit" class="btn btn-primary">Edit</button>
              </form>
            </li>
            {%endif%}
            {%if staff%}
              <li>
                <form class="my-nav-form-pad" action="/update/ticket/{{ticket.id}}" method="get">
                  <button type="submit" class="btn btn-primary">Modifie</button>
                </form>
              </li>
              {%endif%}
              {% if admin %}
                <li>
                  <form class="my-nav-form-pad" action="/create/project" method="get">
                    <button type="submit" class="btn btn-primary">Create New Project</button>
                  </form>
                </li>
              {% endif %}
              <li>
                <form class="my-nav-form-pad" action="/logout" method="POST">
                  {{ csrfField }}
                  <button type="submit" class="btn btn-danger">Logout</button>
                </form>
              </li>
            {% else %}
              <ul class="nav navbar-nav navbar-right ">
                <li>
                  <a id="loginButton" href="/login">LogIn</a>
                </li>
                <li>
                  <a href="/register">Register</a>
                </li>
                {% endif%}
              </ul>
            </div>
            <!-- /.navbar-collapse -->
          </div>
          <!-- /.container-fluid -->
        </nav>
